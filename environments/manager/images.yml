---
# DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

ara_server_tag: "{{ osism_manager_version|default('latest') }}"
ara_server_image: "{{ docker_registry|default('index.docker.io') }}/osism/ara-server:{{ ara_server_tag }}"

ara_web_tag: "{{ osism_manager_version|default('latest') }}"
ara_web_image: "{{ docker_registry|default('index.docker.io') }}/osism/ara-web:{{ ara_web_tag }}"

ceph_ansible_tag: "{{ ceph_version|default('luminous') }}"
ceph_ansible_image: "{{ docker_registry|default('quay.io') }}/osism/ceph-ansible:{{ ceph_ansible_tag }}"

kolla_ansible_tag: "{{ openstack_version|default('rocky') }}"
kolla_ansible_image: "{{ docker_registry|default('quay.io') }}/osism/kolla-ansible:{{ kolla_ansible_tag }}"

mariadb_tag: "{{ osism_manager_version|default('latest') }}"
mariadb_image: "{{ docker_registry|default('index.docker.io') }}/osism/mariadb:{{ mariadb_tag }}"

osism_ansible_tag: "{{ osism_manager_version|default('latest') }}"
osism_ansible_image: "{{ docker_registry|default('quay.io') }}/osism/osism-ansible:{{ osism_ansible_tag }}"

redis_tag: "{{ osism_manager_version|default('latest') }}"
redis_image: "{{ docker_registry|default('index.docker.io') }}/osism/redis:{{ redis_tag }}"
