---
# DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

ara_server_tag: "{{ osism_manager_version|default('latest') }}"
ara_server_image: "{{ docker_registry_service|default(docker_registry)|default('index.docker.io') }}/osism/ara-server:{{ ara_server_tag }}"

ara_web_tag: "{{ osism_manager_version|default('latest') }}"
ara_web_image: "{{ docker_registry_service|default(docker_registry)|default('index.docker.io') }}/osism/ara-web:{{ ara_web_tag }}"

awx_tag: "{{ ceph_version|default('nautilus') }}-{{ openstack_version|default('victoria') }}"
awx_image: "{{ docker_registry_ansible|default(docker_registry)|default('index.docker.io') }}/osism/manager:{{ awx_tag }}"

ceph_ansible_tag: "{{ ceph_version|default('nautilus') }}"
ceph_ansible_image: "{{ docker_registry_ansible|default(docker_registry)|default('index.docker.io') }}/osism/ceph-ansible:{{ ceph_ansible_tag }}"

kolla_ansible_tag: "{{ openstack_version|default('victoria') }}"
kolla_ansible_image: "{{ docker_registry_ansible|default(docker_registry)|default('index.docker.io') }}/osism/kolla-ansible:{{ kolla_ansible_tag }}"

mariadb_tag: "{{ osism_manager_version|default('latest') }}"
mariadb_image: "{{ docker_registry_service|default(docker_registry)|default('index.docker.io') }}/osism/mariadb:{{ mariadb_tag }}"

osism_ansible_tag: "{{ osism_manager_version|default('latest') }}"
osism_ansible_image: "{{ docker_registry_ansible|default(docker_registry)|default('index.docker.io') }}/osism/osism-ansible:{{ osism_ansible_tag }}"

redis_tag: "{{ osism_manager_version|default('latest') }}"
redis_image: "{{ docker_registry_service|default(docker_registry)|default('index.docker.io') }}/osism/redis:{{ redis_tag }}"
